[package]
name = "mc-protocol"
version = "0.1.0"
edition = "2021"
description = "Minecraft Java protocol 776 codecs (VarInt, NBT, packets). No Mojang code."

[dependencies]
thiserror = "2"
bytes = "1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
