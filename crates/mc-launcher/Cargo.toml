[package]
name = "mc-launcher"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Version resolve + cache-only asset bootstrap (never vendors jars)"

[lints]
workspace = true
